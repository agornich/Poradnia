<h3><%= @employee.employee_display2 %></h3>
<h4><%= @employee.employee_specialization %></h4>


SPIS WIZYT:
<br>
  <table class="table panel-body table-striped table-hover">
    <thead>
      <th>ID wizyty</th>
      <th>Data/ godzina wizyty</th>
      <th>Usługa</th>
      <th>Gabinet</th>
      <th>Uwagi</th>
    </thead>
<tbody>
  <% if @employee.visits.count > 0 %>

  <% @employee.visits.each do |p| %>
  <tr>  
    <th><%= p.id %></th>
    <th><%= p.start_at.strftime("Data: %d-%m-%Y, Godz. %H:%M ") %></th>
    <th><%= p.workshop.name %></th>
    <th><%= p.office.name %></th>
    <th><%= p.description %></th>
</tr>
  <% end %>

<% else %>
  brak wizyt dla tego specjalisty
<% end %>
    </tbody>
</table>

